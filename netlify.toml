# Netlify configuration for DailyHabits (Vite + React SPA)

[build]
  command = "npm run build"
  publish = "dist"

# Serve index.html for any path (single-page app)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
